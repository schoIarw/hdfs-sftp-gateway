import{beforeEach,describe,expect,it,vi}from'vitest';import{authenticate,formatBytes,hasCredentials}from'./api';
describe('formatBytes',()=>{it('uses binary units',()=>{expect(formatBytes(0)).toBe('0 B');expect(formatBytes(1024)).toBe('1.00 KiB');expect(formatBytes(1024**4)).toBe('1.00 TiB')});it('rejects invalid values',()=>expect(formatBytes(-1)).toBe('-'))});
describe('authenticate',()=>{const values=new Map<string,string>();beforeEach(()=>{values.clear();vi.restoreAllMocks();vi.stubGlobal('sessionStorage',{getItem:(key:string)=>values.get(key)??null,setItem:(key:string,value:string)=>values.set(key,value),removeItem:(key:string)=>values.delete(key)})});it('stores credentials only after the server accepts them',async()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response(JSON.stringify({username:'admin',roles:['ADMIN']}),{status:200,headers:{'Content-Type':'application/json'}})));await expect(authenticate('admin','correct')).resolves.toEqual({username:'admin',roles:['ADMIN']});expect(hasCredentials()).toBe(true)});it('reports an invalid password without storing credentials',async()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response(null,{status:401})));await expect(authenticate('admin','wrong')).rejects.toThrow('用户名或密码错误');expect(hasCredentials()).toBe(false)});it('distinguishes a server failure from an invalid password',async()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response(null,{status:503})));await expect(authenticate('admin','correct')).rejects.toThrow('Manager 服务异常（HTTP 503）');expect(hasCredentials()).toBe(false)});it('reports a network failure',async()=>{vi.stubGlobal('fetch',vi.fn().mockRejectedValue(new TypeError('network down')));await expect(authenticate('admin','correct')).rejects.toThrow('无法连接 Manager');expect(hasCredentials()).toBe(false)})});
